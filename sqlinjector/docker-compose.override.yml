# Override file for development
version: '3.8'

services:
  sqlinjector:
    volumes:
      - .:/app
      - sqlinjector-cache:/root/.cache
    environment:
      - SQLINJECTOR_DEBUG=true
      - PYTHONPATH=/app
    command: ["--version"]

volumes:
  sqlinjector-cache: